# SOC 科研基础第一周期验证

验证日期：2026-07-21（Asia/Shanghai）

## 已完成

- 冻结 `research_v1` 同化学体系跨数据集协议；
- 建立统一样本契约、双 SOC 标签、泄漏检查、LODO 与 LOCO；
- 对 A123#3、A123#5、CX2_4 执行只读文件资产审计；
- 使用 SHA-256 保护已有模型和正式泛化汇总。

## 真实资产结果

| 数据集 | 文件 | 字节 | 判定 |
|---|---:|---:|---|
| A123#3 | 1 ZIP | 1,002,988,515 | LFP 主研究候选域 |
| A123#5 | 6 XLSX | 1,136,252,416 | LFP 冻结外部域 |
| CX2_4 | 121 TXT + 107 CSV | 112,905,572 | 化学体系不一致，仅兼容性审计 |

共 235 个文件，空文件数为 0。审计产物位于 `E:\SOC电池数据中心\03_模型与实验结果\research_v1\foundation-audit-20260721`。

## 证据边界

本周期完成的是文件资产级质量审计，不代表已经完成三个主实验数据集的逐样本统一。A123#3 与 A123#5 目前仍来自同一 CALCE/A123 数据家族，CX2_4 又不是同一化学体系，因此尚未满足“三个独立同体系数据来源”的最终门槛。下一周期必须引入并解析额外同体系独立数据源。

离线标签使用完整循环信息；在线标签只使用历史电流、固定初始 SOC 和预先给定容量。两者都是参考标签，不宣称为物理真实 SOC。

## 验证命令

```powershell
& '..\..\work\soc_venv\Scripts\python.exe' -m unittest discover -s tests -v
& '..\..\work\soc_venv\Scripts\python.exe' -m compileall -q src tests
```

本周期没有训练或替换任何正式模型。受保护的六个模型/指标文件在审计前后 SHA-256 完全一致。
